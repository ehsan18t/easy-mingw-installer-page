---
const { title, content } = Astro.props;
---

<section class="border-b border-gray-300 px-2 py-10  md:px-4 lg:px-6 bg-gray-100">
  <div class="flex flex-col items-center justify-center gap-3">
    <h2 class="text-3xl font-semibold">{title}</h2>
    <p class="text-justify text-lg text-gray-800">{content}</p>
  </div>
</section>
