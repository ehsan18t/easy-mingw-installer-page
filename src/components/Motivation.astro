---
const { title, content } = Astro.props;
---

<section class="relative pt-28 pb-20">
  <div class="relative z-10 container mx-auto mt-8 max-w-4xl px-4">
    <div class="relative rounded-2xl border border-gray-100/70 bg-white/90 p-8 shadow-lg backdrop-blur-sm md:p-12">
      <!-- Decorative elements -->
      <div class="absolute top-0 left-0 h-20 w-20 -translate-x-1/4 -translate-y-1/4">
        <div class="absolute h-full w-full rounded-full bg-blue-500/20"></div>
        <div class="absolute top-1/2 left-1/2 h-3/4 w-3/4 -translate-x-1/2 -translate-y-1/2 rounded-full bg-indigo-500/25"></div>
      </div>

      <div class="absolute right-0 bottom-0 h-24 w-24 translate-x-1/4 translate-y-1/4">
        <div class="absolute h-full w-full rounded-full bg-blue-500/20"></div>
        <div class="absolute top-1/2 left-1/2 h-2/3 w-2/3 -translate-x-1/2 -translate-y-1/2 rounded-full bg-indigo-500/25"></div>
      </div>

      <div class="relative z-10 flex flex-col items-center justify-center gap-8">
        <!-- Enhanced Title -->
        <div class="mb-2 text-center">
          <span class="mb-3 inline-block rounded-full bg-blue-600/10 px-4 py-1 text-sm font-semibold text-blue-700">About the Project</span>
          <h2 class="relative inline-block text-center text-3xl font-bold text-gray-800 md:text-4xl">
            {title}
            <span class="absolute -bottom-2 left-0 h-1.5 w-full rounded-full bg-gradient-to-r from-blue-600 to-indigo-500"></span>
          </h2>
        </div>

        <!-- Enhanced Quote Box -->
        <div class="relative max-w-3xl rounded-xl border border-gray-100 bg-white p-6 shadow-inner md:p-8">
          <!-- Decorative quotation marks -->
          <div class="absolute -top-6 -left-3 font-serif text-7xl text-blue-400/20">"</div>
          <div class="absolute -right-3 -bottom-16 rotate-180 font-serif text-7xl text-blue-400/20">"</div>

          <p class="px-4 text-center text-lg leading-relaxed font-light text-gray-700 md:text-xl">
            {content}
          </p>
        </div>

        <!-- Enhanced Author signature -->
        <div class="mt-6 flex items-center gap-4 rounded-full border border-blue-100 bg-gradient-to-r from-blue-50 to-indigo-50 px-6 py-3">
          <div class="flex h-12 w-12 items-center justify-center rounded-full bg-gradient-to-br from-blue-600 to-indigo-600 font-bold text-white shadow-md">
            <span class="text-lg">E</span>
          </div>
          <div>
            <div class="font-semibold text-gray-800">Ehsan Khan</div>
            <div class="text-sm text-blue-600">Project Creator</div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bottom decorative elements to connect to the next section -->
  <div class="absolute inset-x-0 bottom-0 h-24 overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-b from-transparent to-gray-100"></div>
  </div>
</section>

<style>
  section {
    position: relative;
    overflow: hidden;
  }

  /* Floating particles to connect with Hero style */
  section::after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 60%;
    background-image: radial-gradient(circle at 20% 30%, rgba(99, 102, 241, 0.15) 0, transparent 50px), radial-gradient(circle at 80% 20%, rgba(59, 130, 246, 0.15) 0, transparent 50px);
    opacity: 0.6;
    z-index: 1;
  }
</style>
